// function runFilms(filmArray) {
//   filmArray.forEach(function (f) {
//     results.innerHTML += `${f.title} <br>`;
//   });
// }

// const results = document.querySelector("#results");

// async function asyncFetch(value) {
//   const res = await fetch(`https://swapi.dev/api/${value}/`);
//   const data = await res.json();
//   displayResults(data, value);
// }
// // EventListener for btns
// let input = document.getElementById("search-input");
// document.querySelector("#btn-login").addEventListener("click", (e) => {
//   asyncFetch(e.target.value.trim().toLowerCase());
//   console.log(e);
// });

// FETCH DATA
// Denna del av koden är till stor del hämtad från föreläsningen om fetch
// Men anpassats för att funka ihop med resterande delar av min egen kod
